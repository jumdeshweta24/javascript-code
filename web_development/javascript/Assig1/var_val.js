/*Declare a variable called myLet using the let keyword and assign it the value 10.Print the
value of myLet to the console using console.log(). Change the value of myLet to 20.Print
the new value of myLet to the console using console.log().
let myLet = 10;
console.log(myLet);
myLet = 20;
console.log(myLet);

//2) Declare a variable called myConst using the const keyword and assign it the value
3.14.Print the value of myConst to the console using console.log().Attempt to change the
value of myConst to 2.71.Observe the error message in the console and write a comment
explaining why the error occurred.
const myConst = 3.14;
console.log(myConst);
myConst = 2.71
console.log(myConst)

//3)Write a program that calculates the area of a circle with a radius of 5. Use the Math.PI
constant and the exponentiation operator( ** ) to perform the calculation.*/
var r = 5;
area_cir = Math.PI * (r ** 2);
console.log(area_cir);